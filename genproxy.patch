diff --git a/genproxy.py b/genproxy.py
index b55c742..8b8c0a1 100755
--- a/genproxy.py
+++ b/genproxy.py
@@ -190,9 +190,14 @@ def do_build() -> None:
         ans = input(f"  Is '{folder.name}' a service? (y/N): ").strip().lower()
         if ans != "y":
             continue
+        compose_path = None
 
-        compose_path = folder / "docker-compose.yml"
-        if not compose_path.exists():
+        for possible_name in ["docker-compose.yml","docker-compose.yaml","compose.yml","compose.yaml"]:
+            if (folder / possible_name).exists(): 
+                compose_path = folder / possible_name
+                break
+
+        if compose_path is None:
             print(f"⚠️  {compose_path.relative_to(BASE_DIR)} not found; skipping.")
             continue
 
@@ -424,4 +429,5 @@ def main() -> None:
         print("Use -h for help.")
 
 if __name__ == "__main__":
-    main()
\ No newline at end of file
+    main()
+
